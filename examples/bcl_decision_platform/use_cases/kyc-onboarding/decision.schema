schema kyc_onboarding {
  required applicant object {
    required sanctions_match bool
    required identity_score number min 0 max 100
    required document_quality number min 0 max 100
    required country string
    optional pep_match bool
  }
}
